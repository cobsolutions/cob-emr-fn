import { DisableDateRangePickerDirective } from './disable.date.range.picker.directive';

describe('DisableDateRangePickerDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableDateRangePickerDirective();
    expect(directive).toBeTruthy();
  });
});
