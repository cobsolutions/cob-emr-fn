import { MultiSelectDisableDirective } from './multi-select-disable.directive';

describe('MultiSelectDisableDirective', () => {
  it('should create an instance', () => {
    const directive = new MultiSelectDisableDirective();
    expect(directive).toBeTruthy();
  });
});
