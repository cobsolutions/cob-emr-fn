import { DispalyInputDirective } from './dispaly-input.directive';

describe('DispalyInputDirective', () => {
  it('should create an instance', () => {
    const directive = new DispalyInputDirective();
    expect(directive).toBeTruthy();
  });
});
