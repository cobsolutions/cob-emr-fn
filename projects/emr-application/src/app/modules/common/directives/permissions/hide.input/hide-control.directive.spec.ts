import { HideControlDirective } from './hide-control.directive';

describe('HideControlDirective', () => {
  it('should create an instance', () => {
    const directive = new HideControlDirective();
    expect(directive).toBeTruthy();
  });
});
